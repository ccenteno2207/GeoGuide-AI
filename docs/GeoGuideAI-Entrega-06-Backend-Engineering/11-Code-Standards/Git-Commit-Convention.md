# Convención de Commits

Se recomienda Conventional Commits:

- feat:
- fix:
- docs:
- test:
- refactor:
- build:
- ci:
- chore:

Ejemplos:
`feat(discovery): add spatial POI search`
`test(places): add PostGIS repository integration tests`
`docs(api): update route discovery contract`
